// English locale. `content.html` is the Korean source of truth for structure;
// this file supplies the English copy that the build overlays onto it.
//
// Keys in `topics` must match the `<section id="...">` values in content.html —
// build.mjs fails if one is missing or unknown. Equations are not duplicated
// here: they are carried over from the source markup, and `{{formula}}` marks
// where each one lands inside a body.
export default {
  lang: 'en',
  output: 'index.html',
  // Nothing Korean may survive into this page.
  forbid: '[가-힣]+',
  title: 'Interactive Differential Equations — Blog',
  description:
    'An interactive introduction to ordinary differential equations and their numerical solvers — direction fields, phase portraits, Euler and Runge–Kutta, numerical stability — and to partial differential equations: transport, continuity and diffusion.',

  // Fills the <h1> and the first index-nav entry.
  heading: 'Interactive Differential Equations',
  languageLink: '<a class="language-link" href="./ko.html" lang="ko">Korean ver.</a>',

  footnote: 'References: Fu &amp; Wang, <em>A Tutorial on Diffusion Theory: From Differential Equations to Diffusion Models</em> (INSAIT), and <a href="https://najeebkhan.github.io//blog/ODE.html" target="_blank" rel="noopener noreferrer">Najeeb Khan, <em>On Differential Equations</em></a>. The preparatory article is <a href="../2026-08-08-interactive-calculus/index.html">Interactive Calculus</a>, and the sequel is <a href="../2026-08-23-sde-and-diffusion/index.html">Interactive Stochastic Differential Equations</a>.',

  navLabel: 'Topics in this article',

  // Replaces everything between the intro:start / intro:end markers.
  intro: String.raw`
      <p class="la-sub">
        Recall the closing scene of the <a href="../2026-08-08-interactive-calculus/index.html">previous article</a>. An arrow was attached to each point of space, and clicking anywhere drew a curve following those arrows. <strong>That curve is the solution of a differential equation.</strong> A differential equation is a rule telling us only "from the current position, in which direction and how fast should we move," and the solution is the trajectory drawn in obedience to that rule.
      </p>
      <p class="la-sub">
        This article starts from that single trajectory and widens the view to how a <strong>whole crowd</strong> of trajectories flows. From an ODE describing the motion of one particle, to a PDE describing how the density of countless particles changes. The bridge joining the two languages is the <strong>continuity equation</strong>, and that is where this article arrives.
      </p>
      <p class="la-sub">
        There are two parts. First, ordinary differential equations and their numerical methods — direction fields and initial value problems, phase portraits, the Euler and Runge–Kutta methods, and numerical stability, which is a different problem from accuracy. Then partial differential equations — the transport, continuity and diffusion equations, and how to translate the language of particles into the language of densities. In the numerical-methods sections you can watch <strong>the solution actually diverge at a step size you choose</strong>.
      </p>
      <p class="la-sub">
        Everything here is deterministic: the same initial value always produces the same trajectory. Mix randomness into that motion and it becomes an SDE, whose density evolution is the Fokker–Planck equation, and at the end of that road lie diffusion models — the story of the <a href="../2026-08-23-sde-and-diffusion/index.html">next article</a>.
      </p>`,

  topics: {
    odeintro: {
      title: 'What is an ODE?',
      lead: 'An equation whose unknown is a function rather than a number. We are given a condition not on the function itself but on its rate of change, and we look for the function satisfying that condition.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> An ordinary differential equation (ODE) is a relation between an unknown function \(x(t)\) and its derivatives. The most common first-order form is \(\frac{dx}{dt}=f(t,x)\), and the degree of the highest derivative is called the order of the equation. "Ordinary" means there is only one independent variable; with several it becomes a partial differential equation.</p><p class="interp"><strong>An equation, but the answer is a function.</strong> The answer to \(x^2-4=0\) is the number \(\pm 2\), but the answer to \(\dot{x}=2x\) is the function \(x(t)=Ce^{2t}\). Moreover there is one for every constant \(C\) — not a single solution but a family of functions. That is why several curves are drawn at once in the demo, and picking one out requires the initial condition of the next section.</p><p class="interp"><strong>A local rule builds a global trajectory.</strong> \(f(t,x)\) tells us only the slope at each point. Drawing those slopes densely as short segments on the plane gives the direction field, and a solution curve is a curve tangent to those segments at every instant. The important point is that without knowing how to solve the equation, the shape of its solutions can be read off this picture alone.</p><p class="interp"><strong>Where do they come from?</strong> Newton's second law \(m\ddot{x}=F\) is a second-order ODE, and the population model \(\dot{P}=kP\) is a direct transcription of "the rate of increase is proportional to the current population." The moment the phrase "rate of change" appears in physics, chemistry, biology or economics, a differential equation is being written.</p><p class="interp"><strong>In machine learning.</strong> In the limit of the learning rate going to 0, gradient descent becomes the ODE \(\dot\theta=-\nabla L(\theta)\) (gradient flow). A Neural ODE takes the network itself as \(f\), solves \(\dot{h}=f_\theta(h,t)\), and uses the solution in place of layers. And the generation process of the diffusion models this article is heading toward is, in the end, solving a single learned ODE.</p>{{formula}}<p class="hint">Change the equation with the presets. The short segments in the background are the direction field, and the coloured curves are solutions corresponding to different constants C. Check that every solution curve is exactly tangent to the segment at its position — that is all the equation says.</p>`,
    },

    ivp: {
      title: 'Initial Value Problems',
      lead: 'The equation alone admits infinitely many solutions. Pinning down the value at a single instant selects exactly one of them, and that uniqueness is what makes simulation meaningful.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> An initial value problem (IVP) gives a differential equation together with an initial condition: \(\dot{x}=f(t,x),\; x(t_0)=x_0\). For an \(n\)-th order equation, \(n\) conditions are needed, from \(x(t_0)\) through \(x^{(n-1)}(t_0)\).</p><p class="interp"><strong>Geometrically: picking one curve.</strong> The direction field is densely paved with non-intersecting solution curves. An initial condition \((t_0,x_0)\) marks a single point on the plane, and the curve through that point is the unique solution. Dragging the point in the demo makes the solution curve slide along with it.</p><p class="interp"><strong>Existence and uniqueness.</strong> It does not work for any \(f\) whatsoever. The Picard–Lindelöf theorem says that if \(f\) is continuous and satisfies a Lipschitz condition in \(x\), then locally a solution exists and is unique. If the Lipschitz condition breaks, uniqueness can fail — \(\dot{x}=\sqrt{|x|},\,x(0)=0\) has both \(x\equiv 0\) and \(x=t^2/4\) as solutions. The presets let you see this case directly.</p><p class="interp"><strong>Finite-time blow-up.</strong> Even when a solution exists, it may not exist forever. The solution of \(\dot{x}=x^2,\,x(0)=1\) is \(x=1/(1-t)\), diverging to infinity as \(t\to 1\). The equation is perfectly well behaved while the solution vanishes in finite time, and this is one of the reasons values become NaN in numerical simulation.</p><p class="interp"><strong>In machine learning.</strong> In a Neural ODE the initial value is the input and the value at the final time is the output. Because uniqueness of the solution is guaranteed, this correspondence is well defined as a function, and thanks to the property that trajectories cannot cross, the map is automatically invertible — which is exactly the grounds on which a continuous-time normalizing flow holds.</p>{{formula}}<p class="hint">Drag the initial point and one solution curve through it is selected. On the "uniqueness failure" preset, two different solutions extend from the same initial point, and on "finite-time blow-up" the solution shoots up vertically at a particular time.</p>`,
    },

    autonomous: {
      title: 'Autonomous and Non-Autonomous Systems',
      lead: 'Whether the rule changes with time divides the character of a system. A time-independent system is far simpler to handle on the stage called phase space, and this distinction explains why diffusion models need a time-conditioned network.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> If \(t\) does not appear explicitly on the right-hand side, as in \(\dot{x}=f(x)\), the system is autonomous; if it does, as in \(\dot{x}=f(t,x)\), it is non-autonomous. An autonomous system is the situation "the laws of physics are the same yesterday and today," while a non-autonomous one has a time-varying force coming in from outside.</p><p class="interp"><strong>An autonomous system is invariant under time translation.</strong> If \(x(t)\) is a solution then so is \(x(t+c)\). So "when did we set out" does not matter, only "where did we set out from." Thanks to this we may erase the time axis and draw only the state space with no loss of information — and that picture is the phase portrait of the next section.</p><p class="interp"><strong>Trajectories do not cross.</strong> In an autonomous system there is only one arrow at each point, so if two trajectories meet at a point they must coincide from then on. Hence distinct trajectories never cross. In a non-autonomous system, by contrast, the arrow at the same location differs by time, so drawing them in \(x\)-space alone rather than the \(x\)-\(t\) plane makes the trajectories appear to cross.</p><p class="interp"><strong>Turning a non-autonomous system autonomous.</strong> Promote time to part of the state. Adding a variable \(s=t\) and writing \(\dot{s}=1,\;\dot{x}=f(s,x)\) removes \(t\) from the right-hand side and gives an autonomous system. It is a trade: one extra dimension in exchange for a simpler theory.</p><p class="interp"><strong>In machine learning: why time is fed as an input.</strong> The noise-prediction network of a diffusion model always takes the time \(t\) as an input, as in \(\epsilon_\theta(x, t)\). That is because the forward noise schedule is a non-autonomous system varying with time, and even for the same \(x\) the job to be done differs entirely between an early and a late stage. Whether or not to feed \(t\) into \(f_\theta(h,t)\) in a Neural ODE bears directly on expressiveness for the same reason.</p>{{formula}}<p class="hint">Alternate between the two systems. In the autonomous system the slope at a given height is the same whatever the time, so each horizontal row of the direction field is identical. In the non-autonomous system the slope at the same height differs by time, so the shape of a solution curve depends on when it started.</p>`,
    },

    phaseportrait: {
      title: 'Phase Portraits',
      lead: 'In an autonomous system we may erase the time axis and draw only the state space. Laying a vector field and trajectories on it shows at a glance the long-run fate of the system — where it converges and what it circles around.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> For a two-dimensional autonomous system \(\dot{x}=f(x,y),\;\dot{y}=g(x,y)\), the picture drawing the vector field \((f,g)\) and representative trajectories on the state space \((x,y)\) is the phase portrait. The vector field of the previous article reappears here unchanged — except that its streamlines now carry the interpretation of "motion in time."</p><p class="interp"><strong>Fixed points.</strong> At points where \(f=g=0\) the arrows vanish. A state placed there never moves, so it is called a fixed point (an equilibrium). The long-run behaviour of a system is largely determined by "which fixed point does it get pulled toward."</p><p class="interp"><strong>Linearization gives the classification.</strong> Approximating the system near a fixed point by its Jacobian gives \(\dot{\delta}\approx J\delta\), and the eigenvalues of \(J\) determine the type. Two negative real eigenvalues give a stable node (drawn in), two positive ones an unstable node, and mixed signs a saddle (attracted along one direction and repelled along another). Complex eigenvalues mix in rotation to give a spiral, with the sign of the real part deciding whether it winds in or unwinds. A single eigenvalue from linear algebra reads off the fate of the dynamics.</p><p class="interp"><strong>Why the eigenvalues decide the fate.</strong> The solution of \(\dot\delta=J\delta\) is of the form \(\delta(t)=e^{\lambda t}v\) along an eigenvector direction. That is, <em>along an eigenvector the motion reduces to a single exponential</em>. So if \(\lambda&lt;0\) that component dies exponentially (drawn in), and if \(\lambda&gt;0\) it grows exponentially (pushed away). A general starting point is a sum of eigenvectors, so each direction shrinks or grows separately according to its own \(\lambda\), and ultimately <strong>the direction with the largest (least negative) \(\lambda\) dominates the picture in the long run</strong>. A trajectory near a saddle approaching along one direction and eventually shooting out along another is the result of this competition.</p><p class="interp"><strong>Why complex eigenvalues mean rotation.</strong> For \(\lambda=a\pm bi\) we have \(e^{\lambda t}=e^{at}(\cos bt \pm i\sin bt)\), so the real part \(a\) controls the magnitude while the imaginary part \(b\) rotates the angle through \(\cos\) and \(\sin\). The two roles multiply to give a spiral that "turns while growing or shrinking." The condition for rotation can be read geometrically too — having a real eigenvector means <em>there exists a line whose direction the transformation preserves</em>, whereas a pure rotation has no such line at all. So the case with no real eigenvector (complex eigenvalues) is exactly the case with rotation mixed in. When \(a=0\) the magnitude does not change and the orbit is a closed ellipse, which is precisely the situation in which GAN training fails to converge and oscillates instead.</p><p class="interp"><strong>Limit cycles.</strong> Fixed points are not the only destinations. Some systems converge to a closed orbit, called a limit cycle. Self-sustaining rhythms such as a heartbeat or predator–prey oscillation have this structure.</p><p class="interp"><strong>In machine learning.</strong> The fixed points of gradient flow \(\dot\theta=-\nabla L\) are the critical points, and its Jacobian is the Hessian. A positive definite Hessian gives a stable node (a local minimum) and mixed signs a saddle point — the critical-point test of the previous article rewritten in the language of dynamics. Meanwhile a system like a GAN, where two objectives compete, has a non-symmetric Jacobian with complex eigenvalues, and so shows spiral oscillation instead of convergence.</p>{{formula}}<p class="hint">Click the figure to draw the trajectory starting from that point. The character of the fixed point differs by preset, and the readout shows the eigenvalues of the Jacobian and the type determined from them. On the predator–prey preset the trajectory draws a closed loop and circulates forever.</p>`,
    },

    euler: {
      title: 'The Euler Method',
      lead: 'Most differential equations cannot be solved by hand. So we approximate the solution by walking a little way along the tangent at a time — the simplest numerical method, and the skeleton of diffusion model samplers.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> \(x_{n+1}=x_n+\Delta t\, f(t_n,x_n)\). Read the slope at the current position, travel straight in that direction for \(\Delta t\), and read the slope again where you land. Repeat.</p><p class="interp"><strong>It is a truncated first-order Taylor expansion.</strong> Keeping only up to the first-order term of \(x(t+\Delta t)=x(t)+\Delta t\,\dot{x}(t)+\frac{\Delta t^2}{2}\ddot{x}+\cdots\) gives the Euler method. The Taylor expansion of the previous article becomes an algorithm here — and what was discarded is the error.</p><p class="interp"><strong>Local and global error.</strong> The error of one step is \(O(\Delta t^2)\), but covering the interval \([0,T]\) takes \(T/\Delta t\) steps, so the accumulated error is \(O(\Delta t)\). Hence the Euler method is called first-order accurate. Halving \(\Delta t\) roughly halves the error, and this slow convergence is what motivated the search for better methods.</p><p class="interp"><strong>Geometrically, why it drifts off.</strong> The Euler method uses only the slope at the start of each step. But the slope changes within the interval, so it is always pushed outward on the side toward which the curve bends. Increasing \(\Delta t\) in the demo shows the approximate trajectory departing systematically to the outside of the true solution — a bias, not a random error.</p><p class="interp"><strong>In machine learning.</strong> The deterministic samplers of diffusion models, DDIM among them, are essentially the Euler method (or a variant) applied to the probability flow ODE. Reducing the number of sampling steps means increasing \(\Delta t\), and this truncation error then shows up directly as degraded image quality. The reason higher-order samplers such as DPM-Solver appeared is in the next section.</p>{{formula}}<p class="hint">Set Δt large and the stepwise approximation (orange) departs noticeably from the true solution (grey). The short tangent drawn at each step shows the rule "read the slope at this point and go straight" exactly. Check in the readout whether halving Δt roughly halves the error.</p>`,
    },

    rungekutta: {
      title: 'Runge–Kutta Methods',
      lead: 'Measuring the slope several times within one step and averaging raises the accuracy dramatically. It means going much further for the same computation, and this is the idea behind higher-order samplers.',
      body: String.raw`<p class="definition"><strong>Definition (RK4).</strong> Each step measures the slope at four places and takes a weighted average: \(k_1\) at the start, \(k_2\) at the midpoint estimated by \(k_1\), \(k_3\) at the midpoint re-estimated by \(k_2\), and \(k_4\) at the end. The final update is \(x_{n+1}=x_n+\frac{\Delta t}{6}(k_1+2k_2+2k_3+k_4)\).</p><p class="interp"><strong>Why the midpoint twice?</strong> The flaw of the Euler method was representing a whole interval by the slope at its start. Using the slope in the middle of the interval gives a much better representative (the midpoint method, second order), and averaging several points with weights cancels still higher terms of the Taylor expansion. The weights \((1,2,2,1)/6\) are the same coefficients as Simpson's rule for integration — not a coincidence but the same principle.</p><p class="interp"><strong>The power of order.</strong> RK4 has global error \(O(\Delta t^4)\). Halving \(\Delta t\) divides the error by sixteen. Each step costs four function evaluations, so a step is four times as expensive, but the number of steps needed for the same accuracy is incomparably smaller, so in practice it is far cheaper. Comparing the three methods at the same \(\Delta t\) in the demo opens this difference by orders of magnitude.</p><p class="interp"><strong>It is not free.</strong> Higher-order methods pay off only when the solution is smooth. If \(f\) changes abruptly or is discontinuous, the assumptions behind the high-order terms break and the order collapses. Also, for the stiff problems of the next section, \(\Delta t\) cannot be increased however high the order — accuracy and stability are separate matters.</p><p class="interp"><strong>In machine learning.</strong> Neural ODEs internally use adaptive RK-family solvers (Dormand–Prince and others), so the "number of layers" is not fixed but determined by the difficulty of the solution. Fast samplers for diffusion models such as DPM-Solver and DEIS are also higher-order methods exploiting the special structure of the probability flow ODE (integrating the linear term exactly and approximating only the nonlinear part), which is how they cut 1000 steps down to 10–20.</p>{{formula}}<p class="hint">Compare the three methods at the same Δt. In the log-scale error graph below, the slope is the order of accuracy — Euler descends with slope 1, the midpoint method with 2, and RK4 with 4. Check that halving Δt divides the RK4 error by sixteen.</p>`,
    },

    stability: {
      title: 'Numerical Stability',
      lead: 'Accuracy and stability are different problems. The numerical solution can oscillate and diverge even while the true solution decays quietly to 0, and the boundary is set by the step size.',
      body: String.raw`<p class="definition"><strong>The test equation.</strong> The true solution of \(\dot{x}=\lambda x\;(\lambda&lt;0)\) is \(x(t)=x_0e^{\lambda t}\), converging to 0. Applying the Euler method gives \(x_{n+1}=(1+\lambda\Delta t)x_n\), so \(x_n=(1+\lambda\Delta t)^n x_0\). For this to go to 0 we need \(|1+\lambda\Delta t|&lt;1\), that is \(\Delta t &lt; 2/|\lambda|\).</p><p class="interp"><strong>What happens past the boundary?</strong> The moment \(\Delta t\) exceeds \(2/|\lambda|\), the absolute value of the multiplier passes 1 and the sign flips every step while the magnitude grows — the true solution decreases monotonically while the numerical one bounces in alternating signs and explodes. Pushing \(\Delta t\) past the critical value in the demo produces this immediately. No amount of extra accuracy solves it.</p><p class="interp"><strong>Stiffness.</strong> Trouble arises when a system contains both a very fast and a very slow component (a large spread in eigenvalue magnitudes). What we care about is the slow behaviour, yet the step size is bound by the stability limit imposed by the fastest component. Such problems are called stiff, and explicit methods are effectively unable to solve them.</p><p class="interp"><strong>Implicit methods.</strong> Backward Euler, \(x_{n+1}=x_n+\Delta t\,f(t_{n+1},x_{n+1})\), uses the slope at the destination. For the test equation the multiplier becomes \(1/(1-\lambda\Delta t)\), whose absolute value is below 1 for \(\lambda&lt;0\) however large \(\Delta t\) is (unconditionally stable). In exchange an equation must be solved at every step, which costs — stability bought with computation.</p><p class="interp"><strong>In machine learning.</strong> The upper bound on the learning rate in gradient descent comes from exactly the same calculation. For a quadratic loss, \(\theta_{n+1}=(I-\eta H)\theta_n\), so the stability condition is \(\eta&lt;2/\lambda_{\max}(H)\). The rule of thumb "if the learning rate is too large the loss oscillates and diverges" is that inequality itself. And the condition number of the previous article reappears here — \(\lambda_{\max}\) binds the learning rate while \(\lambda_{\min}\) sets the speed of convergence, so a large condition number means a stiff problem.</p>{{formula}}<p class="hint">Push Δt past the critical value (the red line). The true solution (grey) heads quietly to 0 while forward Euler (orange) bounces in alternating signs and explodes. Backward Euler (violet) is fine at the same Δt. Making λ more negative narrows the critical value — that is stiffness.</p>`,
    },

    flows: {
      title: 'Flows and Continuous Dynamics',
      lead: 'Regarding a solution not as a trajectory indexed by time but as a map that "flows things forward by time t" reveals new structure. This viewpoint gave rise to Neural ODEs and continuous normalizing flows.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The flow of an autonomous system \(\dot{x}=f(x)\) is the map \(\Phi_t: x_0 \mapsto x(t)\) carrying an initial state to the state at time \(t\). The shift in perspective is regarding it as a transformation moving the whole space at once, rather than a single trajectory.</p><p class="interp"><strong>Group structure.</strong> A flow satisfies \(\Phi_0=\mathrm{id}\) and \(\Phi_s\circ\Phi_t=\Phi_{s+t}\) — flowing for \(t\) and then for a further \(s\) is the same as flowing for \(s+t\). Hence \(\Phi_t^{-1}=\Phi_{-t}\): every flow can be undone by running time backwards. This invertibility is used repeatedly throughout this article.</p><p class="interp"><strong>How does volume change?</strong> The volume of a region carried along by the flow is governed by the divergence: \(\frac{d}{dt}\log V=\nabla\cdot f\). A vector field with zero divergence (incompressible) preserves volume exactly, a positive one expands it, and a negative one contracts it. You can see this in the demo as a square is deformed along the flow.</p><p class="interp"><strong>In machine learning: Neural ODEs.</strong> Taking \(f\) to be a neural network makes \(\Phi_1\) behave like a single layer. The starting point was the observation that ResNet's \(h_{n+1}=h_n+f_\theta(h_n)\) is exactly one Euler step with \(\Delta t=1\) — a residual connection was a discretized flow all along. Instead of a layer count, the integration interval and the solver tolerance set the computation, and memory becomes independent of depth (the adjoint method).</p><p class="interp"><strong>Continuous normalizing flows.</strong> Because a flow is invertible, we can flow a random variable to transform its distribution. The log-density then follows \(\frac{d\log p}{dt}=-\nabla\cdot f\), and the decisive point is that only a trace need be computed rather than a Jacobian determinant. The Probability Flow ODE of the final section has exactly this structure.</p>{{formula}}<p class="hint">Move the time t and the whole grid, along with the square inside it, deforms along the flow. In a divergence-free field the square distorts while keeping its area, and in a contracting field the area shrinks. Turn t negative and it returns exactly to its original place — the invertibility of a flow.</p>`,
    },

    pdeintro: {
      title: 'What is a PDE?',
      lead: 'With two or more independent variables, partial derivatives appear. It is the language required to handle quantities depending on time and space at once — a temperature field, a wave, and a probability density.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> A partial differential equation (PDE) is a relation between an unknown function of several variables and its partial derivatives. Typically one writes, for \(u(t,x)\), something like \(\frac{\partial u}{\partial t}=\alpha\frac{\partial^2 u}{\partial x^2}\) (the heat equation).</p><p class="interp"><strong>How does it differ from an ODE?</strong> The solution of an ODE is a single curve, while the solution of a PDE is an entire function deforming over time. If an ODE is "the motion of one point," a PDE is "the evolution of a whole shape." So the initial condition is not one value but an entire initial function \(u(0,x)\), and if space is finite, boundary conditions are needed too.</p><p class="interp"><strong>Three archetypes.</strong> Second-order linear PDEs fall broadly into three classes. Parabolic (the heat equation \(u_t=\alpha u_{xx}\)) smooths, loses information, and is hard to run backwards in time. Hyperbolic (the wave equation \(u_{tt}=c^2u_{xx}\), the transport equation) propagates at finite speed while preserving shape, and is reversible. Elliptic (Laplace's equation \(\Delta u=0\)) describes a timeless equilibrium. What we will meet in this article are the first two and their combination.</p><p class="interp"><strong>Mostly unsolvable by hand.</strong> Far more so than ODEs. So in practice we chop space into a grid, replace partial derivatives by differences, and integrate the resulting enormous ODE system in time (the method of lines). The demos are computed in exactly this way.</p><p class="interp"><strong>In machine learning.</strong> The protagonist of this article, the Fokker–Planck equation, is a PDE, and it describes how a probability density deforms over time. Understanding diffusion models is, in the end, understanding the relationship between this PDE and the SDE that produces it. Meanwhile the PINN and Neural Operator families, which solve physical equations themselves with neural networks, are an active area.</p>{{formula}}<p class="hint">Apply the three equations to the same initial shape and compare. Transport carries the shape unchanged, diffusion blurs it, and the wave equation splits it left and right. Rewinding the time slider reveals that only diffusion has lost information and cannot restore the original shape.</p>`,
    },

    transport: {
      title: 'Transport / Advection',
      lead: 'The simplest PDE. It carries a shape bodily without changing it, and its solution is given in the explicit form "the initial shape translated."',
      body: String.raw`<p class="definition"><strong>Definition.</strong> \(\frac{\partial u}{\partial t}+c\frac{\partial u}{\partial x}=0\). For a constant speed \(c\) the solution is \(u(t,x)=u_0(x-ct)\), the initial shape pushed to the right by \(ct\). The shape does not change in the slightest.</p><p class="interp"><strong>Characteristics.</strong> Why this is so becomes clear by riding along with the flow. Moving along \(x(t)=x_0+ct\) and watching \(u\) gives \(\frac{d}{dt}u(t,x(t))=u_t+c\,u_x=0\), so to that observer \(u\) is constant. Such curves are called characteristics, and this structure — a PDE reducing to an ODE along characteristics — is the heart of hyperbolic equations.</p><p class="interp"><strong>When the speed varies with position.</strong> If \(c=c(x)\), the characteristics bend and the shape is stretched or compressed. When characteristics collide (a fast part behind catching a slow part in front) the solution begins to break (a shock), which is the difficulty of nonlinear conservation laws.</p><p class="interp"><strong>It loses no information.</strong> Transport is completely reversible. Flip the sign of \(c\), flow for the same time, and you return exactly to the original shape. This is the decisive property contrasting with the diffusion of the next section, and this difference returns in the final section as the question "is deterministic generation possible?"</p><p class="interp"><strong>In machine learning.</strong> The transport equation is a special case of the continuity equation of the next section (density conserved, velocity field constant). And what Flow Matching does is precisely a generalization of it — learning the velocity field carrying the data distribution to a target distribution. It is also where optimal transport theory meets generative models.</p>{{formula}}<p class="hint">Change the speed c and play. In the space–time picture below, the slanted stripes are the characteristics and their slope is the speed. Negative c flows leftward, and rewinding time restores the shape perfectly — transport loses no information.</p>`,
    },

    continuity: {
      title: 'The Continuity Equation',
      lead: 'If nothing is created or destroyed, the rate at which density falls must equal exactly the amount flowing out. This one conservation law is the skeleton of the entire second half of this article.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> For a density \(\rho(t,x)\) and a velocity field \(v(t,x)\), \(\frac{\partial\rho}{\partial t}+\nabla\cdot(\rho v)=0\). Here \(j=\rho v\) is called the flux, and the equation reads "the time change of density + the divergence of the flux = 0."</p><p class="interp"><strong>Why divergence?</strong> Consider a small box. The only way the total inside can fall is by flowing out through the boundary. The net outflow through the boundary was exactly the divergence \(\nabla\cdot j\), so conservation becomes \(\partial_t\rho=-\nabla\cdot j\). The "sources and sinks" picture of the previous article acquires its physical meaning here.</p><p class="interp"><strong>The total is always conserved.</strong> Integrating over all space makes the divergence term 0 (assuming it vanishes at the boundary), giving \(\frac{d}{dt}\int\rho\,dx=0\). Applied to a probability density this means "the total probability is always 1" — the reason the normalization condition of the probability article is automatically maintained under the dynamics.</p><p class="interp"><strong>Relation to the transport equation.</strong> If \(v\) is constant and \(\nabla\cdot v=0\), then \(\nabla\cdot(\rho v)=v\cdot\nabla\rho\) and it reduces to the transport equation of the previous section. Conversely, if \(v\) varies with position, an extra term appears compressing or rarefying the density.</p><p class="interp"><strong>In machine learning: this is the bridge.</strong> When particles move according to the ODE \(\dot{x}=v(x)\), the density of those particles satisfies exactly this continuity equation. That is, the continuity equation is the dictionary joining "the language of trajectories" to "the language of densities." We confirm this directly in "From Particle to Density Dynamics" three sections from here, and the Probability Flow ODE of the final section uses the relationship in reverse — specifying a desired density change and finding the velocity field that produces it.</p>{{formula}}<p class="hint">Play with different velocity fields. The density (blue) is carried along the velocity field, piling up where the flow converges and thinning where it diverges. The bar below is the total mass — check that it stays at 1 however much the shape changes.</p>`,
    },

    heat: {
      title: 'The Heat and Diffusion Equation',
      lead: 'Two names, one equation. The moment the Laplacian governs the time change, every structure is blurred away, and this irreversible smoothing becomes the forward process of a diffusion model.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> \(\frac{\partial u}{\partial t}=\alpha\,\Delta u\). If \(u\) is temperature it is called the heat equation; if it is a concentration or a probability density, the diffusion equation — but mathematically they are exactly the same equation. Here \(\alpha&gt;0\) is the diffusion coefficient.</p><p class="interp"><strong>What the Laplacian does.</strong> In the previous article the Laplacian was "how much lower this point's value is than the average around it." So this equation is a direct transcription of "rise if below your surroundings, fall if above them." Bumps are shaved away and dips filled in, and given enough time everything becomes flat.</p><p class="interp"><strong>The fundamental solution is a Gaussian.</strong> If the initial condition is a delta function concentrated at a point, the solution is \(u(t,x)=\frac{1}{\sqrt{4\pi\alpha t}}e^{-x^2/4\alpha t}\), a Gaussian whose variance grows linearly as \(2\alpha t\). The fact that the width spreads in proportion to \(\sqrt{t}\) reappears unchanged when we meet Brownian motion later — not a coincidence but two faces of the same phenomenon.</p><p class="interp"><strong>Why the fine ripples vanish first.</strong> Recalling that the Laplacian was "the difference from the surrounding average" makes the reason immediately visible. At the same amplitude, a <em>tightly packed</em> ripple travels from crest to trough over a shorter distance and so bends far more sharply, meaning a larger difference from the surrounding average. A larger difference is shaved away that much faster, so fine ripples are blurred out in an instant while only gentle large undulations survive. Confirming this intuition with formulas: the second derivative of a component \(\sin kx\) with wavenumber \(k\) is \(-k^2\sin kx\), so the decay rate is proportional to \(k^2\) and it dies as \(e^{-\alpha k^2 t}\) — <strong>double the frequency and the decay is four times as fast.</strong> Applying a Gaussian blur to a photograph, where fine texture goes first while large blocky forms remain, is the same phenomenon.</p><p class="interp"><strong>It is irreversible.</strong> Diffusion destroys information. The higher the frequency the faster it disappears (as \(e^{-\alpha k^2 t}\) in the wavenumber \(k\)), so after only a little time the fine structure is unrecoverable. The time-reversed equation \(u_t=-\alpha\Delta u\) blows up in the high frequencies and cannot be handled numerically. <strong>And yet diffusion models accomplish exactly this "reverse diffusion"</strong> — how that is possible is the story of the second half of this article (to give the answer away: because they learn the <em>score</em>, extra information about the data distribution).</p><p class="interp"><strong>In machine learning.</strong> The forward process of a diffusion model is blurring the data distribution according to this equation until it becomes a Gaussian. A Gaussian blur in image processing is the heat equation solved for a short time, and on a graph, diffusion by the graph Laplacian appears as message passing in a GNN and as the over-smoothing phenomenon.</p>{{formula}}<p class="hint">Play and the sharp peaks collapse first, followed by the broad structures. In the frequency graph below you can see the high frequencies dying exponentially faster — this is why diffusion is irreversible, and why rewinding does not restore the original shape.</p>`,
    },

    particledensity: {
      title: 'From Particle to Density Dynamics',
      lead: 'The same phenomenon can be written in two languages: as the trajectory of each individual particle (an ODE), or as the density of the crowd (a PDE). The second half of this article is a story about translating between them.',
      body: String.raw`<p class="definition"><strong>Two descriptions.</strong> The Lagrangian view follows the particles: \(\dot{x}_i=v(x_i,t)\). The Eulerian view stands at a fixed location and counts what passes: \(\partial_t\rho+\nabla\cdot(\rho v)=0\). They describe the same physics, but their unknowns are trajectories and a density respectively.</p><p class="interp"><strong>The translation rule.</strong> Moving \(N\) particles along a velocity field \(v\) makes their empirical distribution converge, as \(N\to\infty\), to a solution of the continuity equation. Conversely, given a density, draw particles from it and let them flow. In the demo you can see the upper point cloud (particles) and the lower curve (density) moving together under the same velocity field and agreeing with each other.</p><p class="interp"><strong>Why both languages are needed.</strong> A density holds the complete information of the system but is impossible to store or compute in high dimensions (the same curse-of-dimensionality problem as a joint distribution). Particles can be handled even in high dimensions but are a finite sample and so carry noise. Building the theory in densities and computing with particles is the strategy in practice, and diffusion models work exactly that way — justified by Fokker–Planck and executed on sample paths.</p><p class="interp"><strong>Even deterministic motion spreads a density.</strong> Even if individual particles move without any randomness, the density spreads or gathers whenever the velocity field has divergence. That is, the diffusion of a density does not necessarily come from randomness. This observation is the seed of the final section's Probability Flow ODE — it is why the same density evolution as a random SDE can be reproduced by a deterministic ODE.</p><p class="interp"><strong>In machine learning.</strong> Generative models use this translation in both directions. Training deals with densities (likelihoods, scores), while sampling flows particles (a single path). Flow Matching first fixes a desired density path and then learns by regression the velocity field realizing it, and the correspondence of this section is exactly what makes that possible.</p>{{formula}}<p class="hint">Increase the number of particles N and play. The histogram of the upper particles gradually overlays the solution of the continuity equation below (orange). Lumpy when N is small and smoother as it grows — the convergence seen in the probability article's sampling section, meeting dynamics here.</p>`,
    },
  },

  // Applied outside <pre> blocks: demo labels, buttons, and the initial text of
  // each readout.
  // Applied outside <pre> blocks: demo labels and buttons, plus the Korean
  // inside \text{...} in the shared equations.
  ui: [
    ["보통 (유일)", "Ordinary (unique)"],
    ["유한시간 폭발", "Finite-time blow-up"],
    ["유일성 붕괴", "Uniqueness failure"],
    ["자율계 ẋ = x(1−x)", "Autonomous ẋ = x(1−x)"],
    ["비자율계 ẋ = x·cos t", "Non-autonomous ẋ = x·cos t"],
    ["안정 마디", "Stable node"],
    ["안장", "Saddle"],
    ["나선", "Spiral"],
    ["극한 순환", "Limit cycle"],
    ["포식자–피식자", "Predator–prey"],
    ["궤적 지우기", "Clear trajectories"],
    ["Δt에 따른 오차 (로그–로그) — 직선의 기울기가 정확도 차수", "Error against Δt (log–log) — the slope of the line is the order of accuracy"],
    ["비압축 (∇·f = 0)", "Incompressible (∇·f = 0)"],
    ["수축", "Contracting"],
    ["흘려보내기", "Flow it"],
    ["시공간 전체 — 가로는 x, 세로는 t (아래에서 위로 흐름)", "The whole space–time — x horizontally, t vertically (flowing bottom to top)"],
    ["시공간 — 기울어진 줄무늬가 특성곡선 x = x₀ + ct", "Space–time — the slanted stripes are the characteristics x = x₀ + ct"],
    ["일정 속도", "Constant speed"],
    ["한 점으로 모음", "Gather to a point"],
    ["밀어냄", "Push apart"],
    ["주파수 성분의 감쇠 — 높은 k일수록 e^(−αk²t)로 빠르게 사라진다", "Decay of the frequency components — the higher the k, the faster it dies as e^(−αk²t)"],
    ["파동", "Wave"],
    ["재생", "Play"],
    ["수송", "Transport"],
    ["확산", "Diffusion"],
    ["전단", "Shear"],
    ["\\text{립시츠: }", "\\text{Lipschitz: }"],
    ["\\text{국소적으로 해가 유일}", "\\text{the solution is locally unique}"],
    ["\\text{자율계}", "\\text{autonomous}"],
    ["\\text{비자율계}", "\\text{non-autonomous}"],
    ["\\text{자율화}", "\\text{made autonomous}"],
    ["\\text{(고정점 근처)}", "\\text{(near a fixed point)}"],
    ["\\text{마디 · 안장 · 나선}", "\\text{node · saddle · spiral}"],
    ["\\text{국소 오차}", "\\text{local error}"],
    ["\\text{전역 오차}", "\\text{global error}"],
    ["\\text{(1차 정확도)}", "\\text{(first-order accuracy)}"],
    ["\\text{전진 오일러: }", "\\text{forward Euler: }"],
    ["\\text{후진 오일러: }", "\\text{backward Euler: }"],
    ["\\text{경사하강: }", "\\text{gradient descent: }"],
    ["\\text{포물형 (확산)}", "\\text{parabolic (diffusion)}"],
    ["\\text{쌍곡형 (수송)}", "\\text{hyperbolic (transport)}"],
    ["\\text{타원형 (평형)}", "\\text{elliptic (equilibrium)}"],
    ["\\text{ 는 그 곡선 위에서 상수}", "\\text{ is constant along that curve}"],
    ["\\text{(플럭스)}", "\\text{(flux)}"],
    ["\\text{(총량 보존)}", "\\text{(total mass conserved)}"],
    ["\\text{(점 초기조건)}", "\\text{(point initial condition)}"],
    ["\\text{(고주파가 먼저 죽는다)}", "\\text{(high frequencies die first)}"],
    ["\\text{(입자, 라그랑주)}", "\\text{(particles, Lagrangian)}"],
    ["\\text{(밀도, 오일러)}", "\\text{(density, Eulerian)}"],
  ],

  codeComments: [],
};
