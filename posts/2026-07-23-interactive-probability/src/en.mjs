// English locale. `content.html` is the Korean source of truth for structure;
// this file supplies the English copy that the build overlays onto it.
//
// Keys in `topics` must match the `<section id="...">` values in content.html —
// build.mjs fails if one is missing or unknown. Equations are not duplicated
// here: they are carried over from the source markup, and `{{formula}}` marks
// where each one lands inside a body.
export default {
  lang: 'en',
  output: 'index.html',
  // Nothing Korean may survive into this page.
  forbid: '[가-힣]+',
  title: 'Interactive Probability — Blog',
  description:
    'An interactive introduction to random variables, PMF/PDF, joint and conditional distributions, expectation and variance, the Categorical, Gaussian and Laplace distributions, and the road from likelihood through MLE and NLL to Bayes and MAP.',

  // Fills the <h1> and the first index-nav entry.
  heading: 'Interactive Probability',
  languageLink: '<a class="language-link" href="./ko.html" lang="ko">Korean ver.</a>',

  footnote: 'Reference: <a href="https://www.deeplearningbook.org/contents/prob.html" target="_blank" rel="noopener noreferrer">Goodfellow, Bengio &amp; Courville, <em>Deep Learning</em> — Chapter 3: Probability and Information Theory</a>. Information theory (entropy, KL, cross-entropy) is covered in a separate article.',

  navLabel: 'Topics in this article',

  // Replaces everything between the intro:start / intro:end markers.
  intro: String.raw`
      <p class="la-sub">
        In the earlier <a href="../2026-07-14-linear-algebra/index.html">linear algebra</a> article, data was represented by vectors and matrices, and a model was a linear map transforming those representations. But that language alone cannot explain why feeding the same input to the same model twice can produce a <em>different</em> answer. A language model generates a different sentence each time from the same prompt; a diffusion model produces a different image each time under the same condition. The reason is that we are handling <strong>distributions</strong>, not deterministic functions.
      </p>
      <p class="la-sub">
        There are three broad reasons machine learning needs probability. First, <strong>uncertainty</strong> always remains in a prediction — because the world is inherently stochastic (inherent randomness), because our observations are incomplete (partial observability), or because the model is a simplification (model misspecification). Second, only by quantifying that uncertainty can we judge how much to trust a prediction. Third, if we learn the distribution that generates the data itself, we can <strong>produce new samples</strong> — which is exactly what a generative model does.
      </p>
      <p class="la-sub">
        Probability even determines the shape of the loss function. Cross-entropy for classification, MSE for regression, MAE for robust regression, and L2/L1 regularization each follow as the <strong>negative log-likelihood</strong> of a different probabilistic model. Instead of memorizing loss functions, ask "what distribution is this model assuming?" and the formula follows.
      </p>
      <p class="la-sub">
        This article starts from the definition of a random variable and moves through PMF/PDF and sampling, joint, marginal and conditional probability, the chain rule and independence, expectation, variance and covariance, and on to maximum likelihood estimation, Bayes' rule and MAP. Rather than listing distributions like a catalogue, it treats in depth only the three that touch machine learning losses directly — the <strong>Categorical</strong> distribution of classification, the <strong>Gaussian</strong> of regression, and its alternative the <strong>Laplace</strong>. Each concept is presented as definition, intuition, algebraic form, and use in machine learning.
      </p>`,

  topics: {
    randomvar: {
      title: 'Random Variables',
      lead: 'A random variable is the device that translates "what could happen" into numbers. A die roll, a classifier label, a person\'s height — objects of entirely different kinds are all handled through this single format.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> A random variable \(X\) is a function \(X:\Omega\to\mathbb{R}\) assigning a real number to each outcome \(\omega\) in the sample space \(\Omega\). Despite the name, a random variable is not itself random — it is a function; what is random is which \(\omega\) gets drawn. By convention the variable is written with a capital \(X\) and the value it actually takes with a lowercase \(x\).</p><p class="interp"><strong>Why we need this device.</strong> "The die shows an even number," "this photo is a cat," and "this person is about 170cm tall" are statements of different kinds. A random variable turns all of them into statements about numbers. Once something is a number we can add it, average it, and differentiate it. Machine learning can work with probability because of this translation.</p><p class="interp"><strong>Discrete and continuous.</strong> If the values \(X\) can take are finite or countable, it is a discrete random variable; if they fill a real interval, it is continuous. This distinction is what separates the PMF from the PDF in the next section. In the diagram, the die and the class label are drawn as bars while height is drawn as a smooth curve — that is the difference.</p><p class="interp"><strong>In machine learning.</strong> Almost everything in training is a random variable: the input features \(X\), the label \(Y\), unobserved latent variables \(Z\) (a VAE's code, a mixture component index), and the action \(A\) and reward \(R\) of reinforcement learning. Building a model means specifying the joint or conditional distribution among them.</p>{{formula}}<p class="hint">Press the three buttons. You can compare how an outcome of the sample space (a die face, an image) is carried over to a single number, and why for a continuous variable it is area rather than bar height that becomes probability.</p>`,
    },

    probabilitydistribution: {
      title: 'Probability Distributions',
      lead: 'If a random variable decides what gets observed as a number, a probability distribution decides how often each number appears. A distribution describes both the set of possible values and how probability is spread across them.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The probability distribution of a random variable \(X\) is the rule assigning \(P(X\in A)\) to each set \(A\). Put simply, it collects the answers to every question of the form "how likely is \(X\) to land in this value or range?" For a discrete variable we can list the probability of each value; for a continuous one we describe the distribution by the probability of falling in an interval.</p><p class="interp"><strong>A random variable and a distribution are different things.</strong> The random variable \(X\) is the rule translating outcomes of the sample space into numbers; the distribution is the behavioural pattern describing how probability sits on the results of that translation. By analogy with a program, the random variable is the variable or function determining what gets returned, and the distribution is the statistical pattern its return values make when the code is run many times.</p><p class="interp"><strong>Different random variables can share a distribution.</strong> The variable \(X\) mapping heads to 1 and tails to 0, and the variable \(Y\) mapping exam pass to 1 and fail to 0, are defined on different experiments. But if the probability of heads and of passing are both 0.7, the two follow the very same \(\mathrm{Bernoulli}(0.7)\) distribution. A distribution discards the specific story behind the outcome and keeps only the structure of values and probabilities.</p><p class="interp"><strong>Variables of the same kind can have different distributions.</strong> Two dice may both take values 1 through 6, but if one is fair and the other favours 6, their distributions differ. Knowing the list of possible values is not enough; we must know the probability attached to each value before we can predict the variable's behaviour.</p><p class="interp"><strong>Notation.</strong> \(X\sim p\) or \(X\sim\mathcal D\) reads "\(X\) follows the distribution \(p\) (or \(\mathcal D\))." For \(X\sim\mathcal N(\mu,\sigma^2)\) the family is Gaussian, and \(\mu\) and \(\sigma^2\) are the parameters fixing its location and spread. A collection of distributions sharing one name and differing only in parameters is called a distribution family.</p><p class="interp"><strong>How a distribution is written down.</strong> A distribution is one concept, and the PMF, PDF and CDF are different tools for expressing it. A discrete variable writes the probability of each value directly with a PMF; a continuous variable expresses interval probabilities as areas under a PDF. The CDF can be used for both. The next section looks concretely at the differences among these three representations.</p>{{formula}}`,
    },

    distribution: {
      title: 'PMF, PDF and Probability',
      lead: 'A discrete random variable assigns probability directly to each value (PMF), but for a continuous variable the probability of any single point is zero, so we assign a density and integrate over an interval to get a probability (PDF). The two worlds are not disconnected: narrowing the bin width joins them naturally.',
      body: String.raw`<p class="definition"><strong>Definition (PMF).</strong> The probability mass function \(P(x)\) of a discrete random variable assigns probability directly to each value. There are only two conditions: \(0\le P(x)\le 1\) for every \(x\), and the total \(\sum_x P(x)=1\).</p><p class="definition"><strong>Definition (PDF).</strong> For a continuous random variable the probability of a single point is zero — the probability that a height is exactly 170.000…cm is zero. So instead of probability we assign a density \(p(x)\ge 0\) and obtain probability by integrating over an interval. It suffices that the total integral is \(\int p(x)\,dx=1\).</p><p class="interp"><strong>Density is not probability.</strong> This is the most common misconception. \(P(x)\) can never exceed 1, but \(p(x)\) may exceed 1 freely. The uniform distribution on \([0,\,0.1]\), for instance, has density 10. Density is probability per unit length, so it only becomes a probability once multiplied by a width \(\Delta\): \(P(x\le X\le x+\Delta)\approx p(x)\,\Delta\).</p><p class="interp"><strong>The two worlds are connected.</strong> Slice a continuous distribution into bins of width \(\Delta\) and collect the probability of each cell, and you get a discrete distribution. In the diagram, shrinking \(\Delta\) makes the bars ever finer until they cling to the curve — this is the limit taking a PMF to a PDF. Conversely, discretizing a continuous random variable into bins is always possible, and in practice image pixel values and audio samples are handled exactly that way.</p><p class="interp"><strong>The CDF: one expression binding both.</strong> The cumulative distribution function \(F(x)=P(X\le x)\) is defined identically whether the variable is discrete or continuous. It increases monotonically, going to 0 at \(-\infty\) and 1 at \(+\infty\). An interval probability comes out of a single subtraction: \(P(a\le X\le b)=F(b)-F(a)\). The sampling in the next section uses precisely this function.</p>{{formula}}<p class="hint">Shrink the Δ slider. The bars (a discretized probability) converge onto the curve (a density). Watching the "bar height × Δ = probability" relation in the readout makes the difference between density and probability plain. Use "Show CDF" to see the cumulative form as well.</p>`,
    },

    sampling: {
      title: 'Sampling',
      lead: 'Knowing a distribution and being able to draw samples from it are different abilities. The way a histogram converges to the underlying density as samples accumulate is the fact on which Monte Carlo estimation, SGD minibatches, and generative sampling all rest.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> To draw a sample from a distribution \(p\) is to generate values so that, in the long run, the relative frequency of each value matches \(p\). We write this \(x^{(i)}\sim p(x)\). Knowing the formula for a distribution and being able to sample from it are separate abilities, and without the latter a generative model cannot exist.</p><p class="interp"><strong>Samples reveal the distribution.</strong> A histogram of \(N\) samples approximates the underlying density. For small \(N\) it is lumpy, but it smooths out as \(N\) grows, and the gap between the sample mean and the true expectation shrinks at roughly the rate \(1/\sqrt{N}\). The slowness of this convergence matters: raising precision tenfold requires a hundred times as many samples.</p><p class="interp"><strong>How to draw: inverse transform sampling.</strong> Most pseudorandom generators supply uniform integers or \(\mathrm{Uniform}(0,1)\) numbers as raw material, and numbers from other distributions are made by transforming those. Draw a uniform \(u\) and compute \(x=F^{-1}(u)\), and that \(x\) follows \(p\) — because the CDF spreads the probability mass of the \(x\)-axis evenly onto \([0,1]\) on the \(y\)-axis. For distributions where \(F^{-1}\) is not easy to compute, other methods such as rejection sampling are used.</p><p class="interp"><strong>In machine learning.</strong> Replacing a hard-to-compute expectation with a sample mean is Monte Carlo estimation. The canonical example is SGD, which approximates the full loss by the average over a minibatch drawn uniformly from the dataset. Under such standard uniform-sampling conditions the minibatch gradient is an unbiased estimator of the full-data gradient. The process by which a diffusion model builds an image from noise, and a language model picks tokens one at a time, are both sampling too.</p>{{formula}}<p class="hint">Raise the N slider from 1 to 10,000. The histogram converges onto the orange density curve and the error in the readout shrinks. Press "Resample" and the result differs even at the same N — a sample always carries chance with it. The lower figure shows how a single uniform number becomes a sample from the desired distribution.</p>`,
    },

    joint: {
      title: 'Joint Probability',
      lead: 'Handle two or more random variables at once and probability sits on a grid rather than in a table. The joint distribution describes exhaustively what values the variables take together, and marginalization and conditioning are both just different ways of reading this grid.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The joint distribution \(P(X=x,\,Y=y)\) of two random variables \(X, Y\) is the probability that both take those values simultaneously. In the discrete case it becomes a table on a grid whose cells sum to 1: \(\sum_x\sum_y P(x,y)=1\). In the continuous case the double integral of the joint density \(p(x,y)\) equals 1.</p><p class="interp"><strong>The joint distribution holds everything.</strong> The answer to every question one can ask about \(X\) and \(Y\) is inside this table. The distribution of \(X\) alone, of \(Y\) alone, and of one given the other are all obtained by reading the grid differently. The next three sections — marginalization, conditioning, and the chain rule — are exactly those ways of reading.</p><p class="interp"><strong>The cost is size.</strong> If each variable takes \(K\) values, the grid has \(K^2\) cells, and with \(n\) variables it needs \(K^n\). For a language model with a vocabulary of 50,000, the joint distribution of just two words already runs to 2.5 billion cells. This exponential blow-up is the fundamental problem of probabilistic modelling, and independence assumptions, conditional independence, factorization and neural parameterization are all attempts to dodge it.</p><p class="interp"><strong>In machine learning.</strong> What a generative model is ultimately trying to learn is the joint distribution of the data \(p(x_1,\ldots,x_n)\). A latent-variable model inserts an unobserved \(Z\) to work with \(p(x,z)\), then sums \(z\) away when only the observed part is needed. That operation is the marginalization of the next section.</p>{{formula}}<p class="hint">Drag cells up and down to change their values (arrow keys work too). Raising one cell automatically renormalizes the rest so the total stays 1. Use the preset buttons to compare "correlated / independent / uniform" — this grid keeps reappearing over the next four sections.</p>`,
    },

    marginal: {
      title: 'Marginalization',
      lead: 'Summing an unwanted variable away in a joint distribution leaves the distribution of what remains. This operation — erasing latent variables to obtain the distribution of the observed ones — is where the computational burden of latent-variable models and Bayesian inference arises.',
      body: String.raw`<p class="definition"><strong>Definition (sum rule).</strong> Summing a joint distribution over all values of one variable leaves the distribution of the other. The \(P(x)\) so obtained is called the marginal distribution, and the operation marginalization. In the continuous case the sum becomes an integral; the logic is unchanged.</p><p class="interp"><strong>Why "marginal"?</strong> The name comes from the practice of drawing a grid on paper and writing the sum of each row and column in the margin. In the diagram, summing over \(Y\) leaves each column's total in the bottom margin as \(P(X)\), and summing over \(X\) leaves each row's total in the side margin as \(P(Y)\).</p><p class="interp"><strong>It is an information-destroying operation.</strong> Marginalization cannot be undone. Knowing \(P(x)\) and \(P(y)\) does not recover \(P(x,y)\) — there are infinitely many joint distributions with the same marginals (in the "independence" demo three sections from here, moving \(t\) leaves the marginals unchanged, which is an example). Information about the variable summed away is gone.</p><p class="interp"><strong>In machine learning: this is the bottleneck.</strong> A latent-variable model is defined by \(p(x)=\int p(x,z)\,dz\), and for complex models this integral is often intractable to compute exactly. That is why a VAE optimizes a lower bound called the ELBO instead, and why Bayesian inference depends on MCMC or variational inference because of the evidence \(p(x)=\int p(x\mid\theta)p(\theta)\,d\theta\). Diffusion models likewise introduce many intermediate latent states to convert an intractable data likelihood into stagewise probabilities and a computable training objective.</p>{{formula}}<p class="hint">Use the buttons to change the axis being summed over. The bars in the readout show the sum along that direction, and the total is always 1. Drag the grid cells to change the joint distribution and watch how the marginals follow.</p>`,
    },

    conditional: {
      title: 'Conditional Probability',
      lead: 'A conditional probability is one slice taken out of the joint distribution and renormalized so that it sums to 1 again. This is precisely the distribution supervised learning learns, and it takes the form of an output distribution given an input.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> When \(P(X=x)&gt;0\), the conditional probability of \(Y\) given \(X=x\) is the joint probability divided by the probability of the condition: \(P(y\mid x)=P(x,y)/P(x)\). What the denominator does is normalize — it makes the detached slice sum to 1 again.</p><p class="interp"><strong>Geometrically: cut, then measure again.</strong> From the grid, keep only the column where \(X=x\) and erase the rest. The values in that column are proportions relative to the whole grid, so they do not sum to 1. Dividing by the column total \(P(x)\) turns them into proportions within that column, and now they sum to 1. To condition is to narrow the sample space and then measure probability afresh inside it.</p><p class="interp"><strong>Change the condition and the distribution changes.</strong> Moving the \(X\) slider changes the shape of \(P(Y\mid X=x)\). That change is exactly the information \(X\) carries about \(Y\). If the shape were identical for every \(x\) you chose, \(X\) would tell us nothing about \(Y\) — and that is the independence of two sections from here.</p><p class="interp"><strong>In machine learning: this is what supervised learning learns.</strong> A classifier learns \(p(y\mid x)\), and so does a regression model (usually as a Gaussian). Image captioning is \(p(\text{text}\mid\text{image})\), and conditional generation is \(p(x\mid c)\). What a neural network does, instead of memorizing the whole joint grid, is learn a function that takes an input \(x\) and directly outputs the normalized distribution of that column — which is why it need not store \(K^n\) cells.</p>{{formula}}<p class="hint">Change the condition with the X slider. The selected column is highlighted and the rest dimmed, and the readout shows that column normalized into \(P(Y\mid X=x)\) as bars. Check that the total always returns to 1.</p>`,
    },

    chainrule: {
      title: 'The Chain Rule',
      lead: 'A joint distribution can always be decomposed into a product of conditional probabilities, and the order of that decomposition is not unique. The fact that any order recovers the same joint distribution is why autoregressive models and next-token prediction in language models are legitimate.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Multiplying both sides of the definition \(P(y\mid x)=P(x,y)/P(x)\) by \(P(x)\) and turning it around gives \(P(x,y)=P(x)P(y\mid x)\). With \(n\) variables, repeating this decomposes the joint distribution completely into a product of conditional probabilities. This is the chain rule.</p><p class="interp"><strong>We choose the order.</strong> \(P(x,y)=P(x)P(y\mid x)\) and \(P(x,y)=P(y)P(x\mid y)\) hold simultaneously. The two decompositions store entirely different numbers yet multiply out to the same joint distribution. With \(n\) variables, all \(n!\) orderings are valid. No order is wrong; it is only that some are easy to compute and others hard.</p><p class="interp"><strong>Why this is decisive.</strong> The chain rule splits a joint distribution into \(n\) conditional distributions. However, storing conditional probability tables outright for every possible context still requires an exponential number of values. What lets a language model avoid an enormous table is the chain rule <em>plus</em> a single neural network — shared across every position and context — that computes "the distribution of the next item given what has been seen so far." The chain rule supplies the exact decomposition, and neural parameterization compresses and approximates it.</p><p class="interp"><strong>In machine learning: this is a language model.</strong> Decomposing the probability of a sentence \(P(w_1,\ldots,w_T)\) from left to right gives \(\prod_t P(w_t\mid w_{&lt;t})\). During training the model is only ever asked to predict "the distribution of the next token," and thanks to the chain rule that alone amounts to representing the joint distribution of the whole sentence. PixelCNN, WaveNet and autoregressive diffusion models share the same skeleton, differing only in the axis.</p>{{formula}}<p class="hint">Switch the decomposition-order buttons. The readout shows the numbers each decomposition actually stores (one marginal plus one conditional table) — the numbers differ but the products agree. Click the token strip below and the autoregressive decomposition advances one token at a time, accumulating log-probability.</p>`,
    },

    independence: {
      title: 'Independence',
      lead: 'If two variables are independent, the joint distribution factorizes completely into the product of the individual distributions. Instead of remembering the whole grid we need only the per-axis distributions, so an independence assumption dramatically reduces the number of parameters at the cost of expressiveness.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> \(X\) and \(Y\) are independent when \(P(x,y)=P(x)P(y)\) for all \(x,y\), written \(X\perp Y\). Rewritten conditionally, \(P(y\mid x)=P(y)\) — learning \(x\) changes our belief about \(y\) not at all.</p><p class="interp"><strong>Geometrically: the grid becomes an outer product.</strong> An independent joint distribution is the outer product of a column vector and a row vector. Every row is therefore a constant multiple of every other, and likewise for the columns. Sending \(t\) to 1 in the demo shows the grid settling into exactly that shape. The marginals do not change at all in the process — a visual confirmation of the marginalization section's remark that many joint distributions share the same marginals.</p><p class="interp"><strong>Algebraically: it is compression.</strong> A general \(K\times K\) joint distribution has \(K^2-1\) free parameters, but assuming independence reduces that to \(2(K-1)\). At \(K=5\), 24 becomes 8, and with \(n\) variables \(K^n\) becomes \(nK\). Exponential turns into linear. In exchange, no interaction between the variables can be expressed — compression always has a price.</p><p class="interp"><strong>In machine learning.</strong> Naive Bayes assumes all features are conditionally independent given the class, writing \(p(x\mid y)=\prod_j p(x_j\mid y)\). It is plainly a false assumption, but the parameter count drops dramatically so it trains on little data, and classification performance is often surprisingly serviceable. Treating a dataset as i.i.d., making each latent dimension independent in a VAE (\(p(z)=\prod_j \mathcal{N}(z_j;0,1)\)), and adding independent noise per pixel in a diffusion model are all assumptions of the same kind.</p>{{formula}}<p class="hint">Move the t slider from 0 to 1, or press "Force independence." The upper grid converges to the outer product of the marginals, and the residual grid below, \(P(x,y)-P(x)P(y)\), vanishes to all zeros. A nonzero residual means the joint distribution cannot be recovered from the two marginals alone.</p>`,
    },

    expectation: {
      title: 'Expectation',
      lead: 'Expectation summarizes a distribution into a single representative value. Because the sample mean converges to the expectation as samples accumulate, expected losses and expected rewards that cannot be integrated can be replaced by minibatch averages.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The expectation of a function \(f\) is \(f(x)\) averaged with probability as the weight: \(\mathbb{E}[f(X)]=\sum_x P(x)f(x)\) in the discrete case and \(\mathbb{E}[f(X)]=\int p(x)f(x)\,dx\) in the continuous case. The case \(f(x)=x\) is what is usually called the mean \(\mu\).</p><p class="interp"><strong>Geometrically: a centre of mass.</strong> Regard the density curve as the mass distribution of a plank with no thickness, and the expectation is the position of the fulcrum where it balances. So an expectation need not be a value the distribution actually takes. The expectation 3.5 of a die roll is not on any face, and the expectation 0.7 of a binary label cannot be a label.</p><p class="interp"><strong>Linearity — the most-used property.</strong> \(\mathbb{E}[aX+bY]=a\mathbb{E}[X]+b\mathbb{E}[Y]\) holds always, even when \(X\) and \(Y\) are not independent. This property lets us split the expectation of a sum of losses term by term. Moreover, when the function is smooth enough for differentiation and integration to be exchanged, we may write \(\nabla_\theta\mathbb{E}[f_\theta(X)]=\mathbb{E}[\nabla_\theta f_\theta(X)]\). By contrast \(\mathbb{E}[XY]=\mathbb{E}[X]\mathbb{E}[Y]\) holds only under independence, and that difference is precisely the covariance two sections from here.</p><p class="interp"><strong>In machine learning.</strong> What training minimizes is not the loss on one example but the expected loss \(\mathbb{E}_{(x,y)\sim p_{\text{data}}}[\ell(f(x),y)]\). The objective of reinforcement learning is the expected reward \(\mathbb{E}_\pi[\sum_t \gamma^t r_t]\). Neither true distribution is known, so the integral cannot be done directly and is estimated by a sample mean instead — the law of large numbers is what justifies the substitution.</p>{{formula}}<p class="hint">Press "Draw samples." Samples pile up as ticks in the upper figure, while the lower figure traces the running sample mean converging to the true expectation (orange dashed line). The way it swings wildly at first and gradually settles is the same phenomenon as the noise in a minibatch gradient.</p>`,
    },

    variance: {
      title: 'Variance',
      lead: 'Two distributions with the same mean can look completely different. Variance measures how far values are scattered from the mean, and it expresses the uncertainty of a prediction, the stability of an estimator, and the noise in optimization alike.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Variance is the expectation of the squared deviation from the mean: \(\mathrm{Var}(X)=\mathbb{E}[(X-\mu)^2]\). Expanding gives the computationally convenient form \(\mathbb{E}[X^2]-\mathbb{E}[X]^2\). Because a square was used the units are those of the original value squared, and undoing that gives the standard deviation \(\sigma=\sqrt{\mathrm{Var}(X)}\).</p><p class="interp"><strong>Why square?</strong> The expectation of the plain deviation \(X-\mu\) is always 0 by definition — the two sides cancel. To prevent that cancellation the sign must go, and the reason for squaring rather than taking an absolute value is that squares are differentiable and fall out in closed form. Choosing the absolute value gives the mean absolute deviation, and that choice leads later to MAE and the Laplace distribution.</p><p class="interp"><strong>Geometrically.</strong> The two curves in the diagram have the same mean and differ only in variance. Small variance makes a tall narrow peak; large variance a low broad one. The total area is always 1, so width and height must be traded against each other. The \(\pm\sigma\) markers show that width directly.</p><p class="interp"><strong>Why averaging reduces the wobble.</strong> The last line of the formula above, \(\mathrm{Var}(\bar{x}_N)=\mathrm{Var}(X)/N\), is the fact reused most often in this article. The reason takes two lines. By the scaling rule \(\mathrm{Var}(aX)=a^2\mathrm{Var}(X)\), the leading coefficient \(1/N\) of \(\bar{x}_N=\frac{1}{N}\sum_i x^{(i)}\) comes out squared as \(1/N^2\), and if the samples are independent the variance of the sum simply adds to \(N\cdot\mathrm{Var}(X)\). Multiplying the two gives \(\frac{1}{N^2}\cdot N\,\mathrm{Var}(X)=\mathrm{Var}(X)/N\). <em>Intuitively</em>, the errors of the individual samples point in different directions and partly cancel one another — if they were all wrong in the same direction (perfectly correlated), no cancellation would occur and this gain would vanish.</p><p class="interp"><strong>Which is where √N comes from.</strong> If the variance falls as \(1/N\), the standard deviation falls as its square root \(1/\sqrt{N}\). What we actually feel as error is the standard deviation, not the variance, so the conclusion of the sampling section — that <strong>raising precision tenfold requires a hundred times as many samples</strong> — follows from here. It is the same \(\sqrt{N}\) that makes quadrupling the minibatch size reduce gradient noise only by half rather than fourfold, and that gives diminishing returns to larger batches.</p><p class="interp"><strong>In machine learning: three faces.</strong> First, the <strong>predictive uncertainty</strong> a model outputs — learning \(\sigma\) alongside a Gaussian output lets the model say how confident it is. Second, the <strong>variance of an estimator</strong> — in the bias–variance decomposition, overfitting is the high-variance state. Third, the <strong>noise of optimization</strong> — a minibatch gradient is an unbiased estimator of the true gradient but carries variance, and increasing the batch size by a factor \(B\) reduces that variance by roughly \(1/B\). This is why the learning rate and batch size have to be tuned together.</p>{{formula}}<p class="hint">Move the two σ sliders. The mean is pinned at 0, so only the spread changes. Making one very small and the other very large makes clear how different a story "same mean, different distribution" can tell.</p>`,
    },

    covariance: {
      title: 'Covariance and Correlation',
      lead: 'Covariance carries the direction and magnitude with which two variables move together; the correlation coefficient strips the magnitude and keeps only the direction. The axes of the ellipse drawn by the covariance matrix are its eigenvectors, and that is what PCA does.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Covariance is the average of the product of the two deviations: \(\mathrm{Cov}(X,Y)=\mathbb{E}[(X-\mu_X)(Y-\mu_Y)]=\mathbb{E}[XY]-\mathbb{E}[X]\mathbb{E}[Y]\). As the second form says, covariance measures how badly \(\mathbb{E}[XY]=\mathbb{E}[X]\mathbb{E}[Y]\) fails — exactly the difference deferred in the previous section.</p><p class="definition"><strong>Definition (correlation).</strong> Covariance depends on units. Changing height from cm to m changes the value a hundredfold. Dividing by each standard deviation removes that dependence, giving the correlation coefficient \(\rho=\mathrm{Cov}(X,Y)/(\sigma_X\sigma_Y)\), which always satisfies \(-1\le\rho\le 1\).</p><p class="interp"><strong>Geometrically: the ellipse's axes are eigenvectors.</strong> The contours of a two-dimensional Gaussian are ellipses, and the shape of that ellipse is determined by the covariance matrix \(\Sigma\). Eigendecomposing \(\Sigma\) makes the eigenvectors the principal axis directions of the ellipse and the eigenvalues the variance along those directions. Raising \(\rho\) tilts the ellipse; returning it to 0 aligns the axes with the coordinate axes. This is exactly what PCA does — finding the direction of greatest spread in the data is the same problem as finding the leading eigenvector of the covariance matrix.</p><p class="interp"><strong>Zero correlation is not independence.</strong> Independence implies zero correlation, but not conversely. If \(X\sim\mathcal{N}(0,1)\) and \(Y=X^2\), then \(Y\) is completely determined by \(X\) and yet \(\mathrm{Cov}(X,Y)=\mathbb{E}[X^3]=0\). The reason is that correlation captures only linear relationships. This is why "the features have been decorrelated" does not mean "the features are independent" in representation learning.</p><p class="interp"><strong>In machine learning.</strong> The entire shape of a multivariate Gaussian \(\mathcal{N}(\mu,\Sigma)\) is held in \(\Sigma\). PCA changes coordinates to the eigenvectors of \(\Sigma\) to reduce dimension, and whitening turns \(\Sigma\) into the identity. Techniques that regularize the covariance between features to prevent representation collapse in contrastive learning (Barlow Twins, VICReg) also work on this matrix directly.</p>{{formula}}<p class="hint">Move the ρ slider from −0.95 to 0.95. The point cloud and the contour ellipse tilt together, and the red and violet axes (the eigenvectors) follow that direction. Leaving ρ at 0 and changing only σx and σy keeps the axes on the coordinate axes and changes just the ellipse's aspect ratio.</p>`,
    },

    categorical: {
      title: 'The Categorical Distribution',
      lead: 'A distribution over a finite set of choices is determined by a single probability vector, and the space that vector can occupy is the simplex of vectors summing to 1. Softmax is the map carrying whatever logit vector a network emits onto that simplex, and both classification and token prediction happen there.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The distribution of a random variable taking \(K\) values is determined by a single probability vector \(\boldsymbol{p}=(p_1,\ldots,p_K)\). The conditions are \(p_k\ge 0\) and \(\sum_k p_k=1\). The special case \(K=2\) is the Bernoulli distribution, covering coin flips and binary classification.</p><p class="interp"><strong>Geometrically: the simplex.</strong> The set of points satisfying both conditions is a \((K-1)\)-dimensional figure sitting in \(K\)-dimensional space. For \(K=3\) it is the triangle joining three vertices, called the probability simplex. The vertices are one-hot distributions (certainty) and the exact centre is the uniform distribution (complete ignorance). A probability vector a model outputs must always lie on this figure, and its degrees of freedom are \(K-1\), not \(K\).</p><p class="interp"><strong>Softmax: the map onto the simplex.</strong> The last layer of a network emits an unconstrained real vector (the logits) \(\boldsymbol{z}\). Softmax \(p_k=e^{z_k}/\sum_j e^{z_j}\) carries it onto the simplex. It does two things — \(\exp\) makes every component positive, then dividing by the total (the L1 norm) forces the sum to 1. That adding a constant to \(\boldsymbol{z}\) leaves the result unchanged (shift invariance) corresponds exactly to the fact that there are \(K-1\) degrees of freedom.</p><p class="interp"><strong>Temperature.</strong> In \(p_k \propto \exp(z_k/T)\), the parameter \(T\) controls how peaked the distribution is. As \(T\to 0\) the probability piles onto the single argmax and becomes one-hot (greedy decoding); as \(T\to\infty\) it flattens to the uniform distribution. This is the very knob that tunes generation diversity in a language model.</p><p class="interp"><strong>Connection to the loss function.</strong> When the correct answer is class \(c\), the negative log-likelihood of this distribution is \(-\log p_c\). That is the cross-entropy loss itself — because for a one-hot target \(y\), \(-\sum_k y_k \log p_k = -\log p_c\). In other words, training a classifier is solving the MLE of a categorical distribution. Take the whole vocabulary as the classes and it becomes next-token prediction in a language model.</p>{{formula}}<p class="hint">Lower the temperature T to 0.1 — the probability piles onto one class. Raise it to 3 and the five classes become nearly uniform. Note that the logits are unchanged while only the distribution moves, and that the sum is always 1.</p>`,
    },

    gaussian: {
      title: 'The Gaussian Distribution',
      lead: 'Determined by just two parameters, a mean and a variance, it arises naturally whenever many factors add together. Using MSE in regression is precisely the same statement as assuming the noise is Gaussian.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The density of a Gaussian (normal) distribution with mean \(\mu\) and variance \(\sigma^2\) is \(p(x)=\frac{1}{\sqrt{2\pi\sigma^2}}\exp\!\left(-\frac{(x-\mu)^2}{2\sigma^2}\right)\). Two numbers determine the whole distribution, and the leading coefficient is only the normalizing constant making the integral 1 — it plays no part in the shape.</p><p class="interp"><strong>Why this distribution in particular?</strong> There are two reasons. First, the central limit theorem — adding together many independent random variables makes the sum approach a Gaussian whatever the individual distributions were. This is why quantities accumulated from countless small factors, such as measurement noise, tend to follow a Gaussian. Second, maximum entropy — when we know only the mean and the variance and nothing else, the distribution embodying the fewest assumptions is the Gaussian. It is the choice that refrains from inventing what we do not know.</p><p class="interp"><strong>Geometrically.</strong> \(\mu\) fixes the location of the peak and \(\sigma\) its width. Changing \(\mu\) slides the shape left and right unchanged; changing \(\sigma\) widens or narrows it in place. For any \(\mu,\sigma\), about 68% falls within \(\pm 1\sigma\) and about 95% within \(\pm 2\sigma\) — the two shaded bands in the diagram.</p><p class="interp"><strong>MSE is a Gaussian assumption.</strong> Assuming \(y=f(x)+\varepsilon\) with \(\varepsilon\sim\mathcal{N}(0,\sigma^2)\) in regression gives \(p(y\mid x)=\mathcal{N}(y; f(x), \sigma^2)\). Taking the negative log brings the square inside the exponent straight down: \(-\log p(y\mid x)=\frac{(y-f(x))^2}{2\sigma^2}+\text{const}\). With \(\sigma\) fixed this is identical to MSE up to a constant factor. When we use mean squared error, we are saying the noise is Gaussian.</p><p class="interp"><strong>In machine learning.</strong> A VAE's latent prior is \(\mathcal{N}(0,I)\) and its encoder outputs \(\mathcal{N}(\mu(x),\sigma(x)^2)\). A diffusion model is itself the process of progressively adding Gaussian noise to data and undoing it, and the fact that each step's transition distribution is Gaussian is what makes training closed-form. Weight initialization, Gaussian noise as an alternative to dropout, and the approximate posterior of a Bayesian neural network all come from here too.</p>{{formula}}<p class="hint">Move μ and σ. The shading marks the ±1σ (68%) and ±2σ (95%) intervals, and those proportions do not change as σ grows. Use "Draw samples" to check with tick marks what fraction of actual samples falls in those intervals.</p>`,
    },

    laplace: {
      title: 'The Laplace Distribution',
      lead: 'It centres in the same place as a Gaussian but has heavier tails and a sharper peak. From the loss-function point of view, that single difference appears as the difference between the parabola of MSE and the V of MAE.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> The density of a Laplace distribution with location \(\mu\) and scale \(b\) is \(p(x)=\frac{1}{2b}\exp\!\left(-\frac{|x-\mu|}{b}\right)\). Set beside a Gaussian, the difference is in exactly one place inside the exponent — the square has become an absolute value. Its variance is \(2b^2\).</p><p class="interp"><strong>Geometrically: a sharp peak, heavy tails.</strong> The figure overlays the two distributions with their variances matched, so only shape is being compared, not width. The Laplace has a sharp, non-differentiable corner at the centre, and in exchange both tails decay more slowly. Turning on "View tails on a log scale" makes it plain that the Gaussian falls as a parabola (fast, like a square) while the Laplace falls as a straight line (slow, like an absolute value).</p><p class="interp"><strong>What heavy tails mean.</strong> Under a Gaussian, an observation \(5\sigma\) away is effectively impossible, so the model drags its mean hard toward it in order to explain it. Under a Laplace the same observation is "rare but plausible," and the fit moves far less. Robustness to outliers comes from the thickness of the tail.</p><p class="interp"><strong>MAE is a Laplace assumption.</strong> Simply repeat the calculation of the previous section. Since \(-\log p(x)=\frac{|x-\mu|}{b}+\log 2b\), up to a constant factor this is the mean absolute error (MAE, the L1 loss). Switching a regression loss from MSE to MAE is not tinkering with a hyperparameter; it is replacing the noise model from Gaussian with Laplace.</p><p class="interp"><strong>In machine learning.</strong> The first use is MAE and the Huber loss in regression with outliers; the second is sparsity. Placing a Laplace prior on the parameters makes the penalty \(\lambda|w|\), that is L1 regularization (Lasso), and the sharp corner gives rise to solutions that are exactly zero. That story is verified directly in the final section.</p>{{formula}}<p class="hint">Move b and compare the two curves — the variances are always matched, so all that changes is shape. The lower figure is the key one: plot −log p and the Gaussian becomes a parabola (MSE) while the Laplace becomes a V (MAE). The two loss functions turn out to have been two distributions.</p>`,
    },

    likelihood: {
      title: 'Likelihood',
      lead: 'Read the same formula as a function of the data and it is a probability; read it as a function of the parameters and it is a likelihood. Choosing the parameters that make the observed data most plausible is maximum likelihood estimation, and most training amounts to that.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Take the single formula \(p(x\mid\theta)\). Holding \(\theta\) fixed and reading it as a function of \(x\) gives a <em>probability (density)</em>; holding \(x\) fixed and reading it as a function of \(\theta\) gives the <em>likelihood</em> \(L(\theta)=p(x\mid\theta)\). The formula is the same; only which argument is treated as the variable differs.</p><p class="interp"><strong>A likelihood is not a distribution.</strong> This is the crucial distinction. \(\int p(x\mid\theta)\,dx=1\) holds, but there is no reason whatever for \(\int L(\theta)\,d\theta\) to be 1. That is why the area under the lower curve is not 1. A likelihood is only a score expressing <em>relative preference</em> among values of \(\theta\); it is not a probability distribution over \(\theta\). To give \(\theta\) genuine probability we need a prior, and that is the Bayes section further on.</p><p class="interp"><strong>With several data points.</strong> If the observations are i.i.d., the joint likelihood is the product of the individual ones: \(L(\theta)=\prod_i p(x^{(i)}\mid\theta)\). Choosing the \(\theta\) that maximizes this product is <strong>maximum likelihood estimation (MLE)</strong>. It is the principle "choose the explanation that makes the observed data most plausible," and training in supervised learning, language models and generative models is almost always of this form.</p><p class="interp"><strong>The Gaussian case: the answer is the sample mean.</strong> With \(p(x\mid\mu)=\mathcal{N}(x;\mu,\sigma^2)\) we have \(\log L(\mu)=-\frac{1}{2\sigma^2}\sum_i (x^{(i)}-\mu)^2+\text{const}\), so differentiating in \(\mu\) and setting to zero gives \(\hat\mu=\frac{1}{N}\sum_i x^{(i)}\). We do not use the mean because "of course it is the mean," but <em>because the MLE solution under a Gaussian assumption is the mean</em>. Change the noise model to a Laplace and the same calculation answers with the median.</p><p class="interp"><strong>The question MLE answers.</strong> MLE asks, "what should the parameters be for the data we just observed to be as plausible as possible?" Because it compares likelihoods only, it uses no knowledge or preference held before seeing the data. It is powerful and simple when there is enough data, but with little data or many parameters it can fit the observed sample too closely.</p>{{formula}}<p class="hint">Click the upper figure to add data points. The likelihood curve \(L(\mu)\) below changes in real time, and its maximum (the green line) always sits exactly at the sample mean of the data. Piling points on one side pulls the curve that way.</p>`,
    },

    mle: {
      title: 'Maximum Likelihood Estimation',
      lead: 'If a likelihood is the score a parameter receives, maximum likelihood estimation adopts as the answer whichever parameter scores highest. Depending on which distribution family is chosen, the same principle yields estimators as different as the sample mean, the median, and the relative frequency.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Given observed data \(\mathcal{D}=\{x^{(1)},\ldots,x^{(N)}\}\) and a model \(p(x\mid\theta)\) indexed by a parameter \(\theta\), the <strong>maximum likelihood estimator (MLE)</strong> is the parameter maximizing the likelihood: \(\hat\theta_{\text{MLE}}=\arg\max_{\theta\in\Theta}L(\theta)\), where \(L(\theta)=p(\mathcal{D}\mid\theta)\). If the observations are i.i.d. then \(L(\theta)=\prod_i p(x^{(i)}\mid\theta)\), and since \(\log\) is strictly increasing, the <em>same</em> \(\hat\theta\) may be found as the maximizer of the log-likelihood \(\ell(\theta)=\sum_i \log p(x^{(i)}\mid\theta)\).</p><p class="interp"><strong>What is held fixed and what moves.</strong> The distinction of the previous section now does real work. The data \(\mathcal{D}\) has already been observed and is a <em>fixed constant</em>; the only thing that moves is \(\theta\). So MLE does not ask "how might data come out" but <strong>"what explanation makes the data that already came out least surprising?"</strong> It shows the same data to many candidate values of \(\theta\), asks each "what probability does your worldview assign to this data?", and adopts the candidate that names the largest number.</p><p class="interp"><strong>The procedure.</strong> In practice one almost always walks the same four steps. ① Choose a distribution family \(p(x\mid\theta)\) (this is the <em>assumption</em>, and it determines every answer that follows). ② Write down the log-likelihood \(\ell(\theta)=\sum_i\log p(x^{(i)}\mid\theta)\). ③ Solve \(\nabla_\theta\ell=0\) (this equation is called the likelihood equation). ④ Confirm the solution is a maximum via a second-order condition or the concavity of the function. When there is no closed form, step ③ is replaced by gradient descent — and <em>that is neural network training</em>.</p><p class="interp"><strong>Three standard examples.</strong> The same procedure produces statistics we already know. For a <strong>Bernoulli</strong>, \(p(x\mid\theta)=\theta^x(1-\theta)^{1-x}\), solving \(\ell'(\theta)=0\) gives \(\hat\theta=\frac{1}{N}\sum_i x^{(i)}\), the <em>observed relative frequency</em> — flip a coin ten times and see seven heads, and the MLE is 0.7. For a <strong>Gaussian</strong>, \(\hat\mu\) is the <em>sample mean</em> and \(\hat\sigma^2=\frac{1}{N}\sum_i(x^{(i)}-\hat\mu)^2\) the <em>sample variance</em>. For a <strong>Laplace</strong>, one must minimize \(\sum_i|x^{(i)}-\mu|\), so the answer is the <em>median</em>. The values we took to be "obvious summaries" were in fact MLE solutions under particular distributional assumptions.</p><p class="interp"><strong>Intuition: choosing the family is choosing the estimator.</strong> The difference among those three results comes not from the data but from the <em>assumption</em>. A Gaussian penalizes squared error, so a single distant point protests in proportion to \((x-\mu)^2\) and the mean is dragged toward it. A Laplace penalizes only the absolute value, so however far away the point is the strength of its protest is constant, and the median does not budge. Drag an outlier in the demo and you can watch the two estimates separate. The message of the earlier sections — that <strong>choosing a loss function is choosing a noise model</strong> — becomes, under the single principle of MLE, another way of saying the same sentence.</p><p class="interp"><strong>Good properties.</strong> There are reasons MLE is used so widely. <em>Consistency</em> — if the model contains the true distribution, \(\hat\theta_N\) converges to the true value as the sample grows. <em>Asymptotic normality</em> — for large \(N\) the distribution of \(\hat\theta\) approaches a Gaussian around the true value whose width is set by the Fisher information, which yields confidence intervals. <em>Invariance</em> — to estimate \(g(\theta)\) there is no need to solve again; \(g(\hat\theta_{\text{MLE}})\) will do. That the MLE of the standard deviation is the square root of the MLE of the variance is an instance.</p><p class="interp"><strong>Three limitations.</strong> First, <em>it can be biased</em>. The MLE of a Gaussian variance divides by \(N\), and because it measures deviations against the \(\hat\mu\) already fitted from the data, it comes out systematically smaller than the true value (\(\mathbb{E}[\hat\sigma^2]=\frac{N-1}{N}\sigma^2\)). The familiar sample variance dividing by \(N-1\) is a <em>different</em> estimator that corrects this bias. Second, <em>overfitting</em> — with little data or many parameters the likelihood conforms too closely to the observed sample. In the extreme, if an event is never observed, MLE assigns it probability exactly 0, and as the information theory article shows, the loss becomes infinite the moment that event actually occurs. Third, <em>it is a point estimate</em> — it returns a single value and says nothing about how confident it is.</p><p class="interp"><strong>Which leads to the following sections.</strong> Maximizing the product directly breaks down numerically, so we take a log to turn it into a sum and flip the sign — that is the NLL of the next section, the quantity actually minimized in implementations. And addressing the three limitations above requires putting a prior on \(\theta\), which is Bayes' rule and MAP. In fact, <strong>setting the prior to be uniform in MAP returns exactly MLE</strong> — MLE is the special case of MAP that declares "all parameter values are equally preferred."</p><p class="interp"><strong>In machine learning.</strong> Nearly every standard supervised loss is a conditional MLE. For a conditional model \(p(y\mid x,\theta)\) one solves \(\hat\theta=\arg\max_\theta\sum_i\log p(y^{(i)}\mid x^{(i)},\theta)\), and inserting a categorical distribution gives cross-entropy, a Gaussian gives MSE, and a Laplace gives MAE. Next-token prediction in a language model is the MLE of a categorical distribution over the vocabulary, extended to a whole sentence by the chain rule, and normalizing flows and autoregressive generative models likewise maximize the log-likelihood directly. "Training a model" is usually shorthand for this sentence.</p>{{formula}}<p class="hint">Switch between Gaussian and Laplace with the family buttons. The data is the same, yet the MLE solution moves from the sample mean to the median. Click near the right edge of the upper figure to add an <strong>outlier</strong>, and watch the Gaussian solution get dragged while the Laplace solution barely moves — a distributional assumption is an attitude toward outliers.</p>`,
    },

    nll: {
      title: 'Log-Likelihood and NLL',
      lead: 'A likelihood is a product of probabilities, so it collapses numerically to zero as soon as the data grows even slightly. This single transformation — taking a log to turn the product into a sum — is why machine learning loss functions are almost without exception of negative log-likelihood form.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Taking the log of the likelihood gives the log-likelihood \(\ell(\theta)=\log L(\theta)=\sum_i \log p(x^{(i)}\mid\theta)\), and flipping the sign to make it a minimization problem gives the negative log-likelihood (NLL) \(-\ell(\theta)\). Since by optimization convention a loss is minimized, it is the latter we use.</p><p class="interp"><strong>Reason 1: products collapse numerically.</strong> If each \(p(x^{(i)})\) is about 0.2, then at \(N=100\) the product is \(10^{-70}\), and at \(N=500\) it passes the smallest magnitude float64 can represent (\(\approx 10^{-308}\)) and becomes exactly 0. The moment it hits 0 we can no longer compare which \(\theta\) is better, and the gradient dies too. Taking a log turns the product into a sum and the problem disappears wholesale.</p><p class="interp"><strong>Reason 2: the argmax is preserved.</strong> Because \(\log\) is strictly increasing, \(L(\theta_1)&gt;L(\theta_2)\) and \(\log L(\theta_1)&gt;\log L(\theta_2)\) are equivalent. So taking the log does not change the optimum in the slightest. It is a nearly free transformation that swaps in an easier objective while leaving the answer alone.</p><p class="interp"><strong>Reason 3: the derivative decomposes.</strong> The derivative of a product tangles terms together through the product rule, but the derivative of a sum is the sum of the term derivatives. So \(\nabla_\theta \ell = \sum_i \nabla_\theta \log p(x^{(i)}\mid\theta)\), and approximating that sum with a minibatch is SGD. The earlier logic — linearity of expectation leading to minibatch estimation — is reused verbatim here.</p><p class="interp"><strong>And so every loss function is an NLL.</strong> Gathering what we have seen produces the table below — choosing a loss function is really choosing a distribution.</p>{{formula}}<p class="hint">Raise the N slider. The "product" in the readout shrinks rapidly and at some point collapses to 0 (underflow), while the "log sum" descends linearly as though nothing had happened. The red vertical line marks where float64 gives up on the product.</p>`,
    },

    bayes: {
      title: "Bayes' Rule",
      lead: 'Multiply a prior by a likelihood and normalize, and you have a posterior. It is the rule describing how belief held before observing data is updated by observation, and it is nothing more than the definition of conditional probability turned around.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Decomposing \(P(x,\theta)\) in two different orders (the chain rule) gives \(P(\theta\mid x)P(x)=P(x\mid\theta)P(\theta)\), and dividing both sides by \(P(x)\) yields Bayes' rule: \(P(\theta\mid x)=\frac{P(x\mid\theta)P(\theta)}{P(x)}\). It is not a new axiom but merely the definition of conditional probability turned around.</p><p class="interp"><strong>The names of the four pieces.</strong> \(P(\theta)\) is the belief before seeing data, the prior; \(P(x\mid\theta)\) is the likelihood of the previous section; \(P(\theta\mid x)\) is the belief after seeing data, the posterior; and the denominator \(P(x)=\int P(x\mid\theta)P(\theta)\,d\theta\) is the evidence. The evidence does not depend on \(\theta\) and is a normalizing constant, so it is usually written only as \(P(\theta\mid x)\propto P(x\mid\theta)P(\theta)\) — but that integral is precisely the marginalization bottleneck seen earlier.</p><p class="interp"><strong>How it differs from MLE.</strong> MLE looks only at the likelihood and picks a single \(\theta\). Bayes multiplies by a prior and obtains not one point but a whole distribution. So it can answer not only "the most plausible \(\theta\)" but also "how certain that estimate is." The less data there is, the broader the posterior and the closer to the prior; as data accumulates it narrows and the likelihood takes over.</p><p class="interp"><strong>The example: estimating a coin's bias.</strong> Let \(\theta\) be the probability of heads and take the prior to be \(\mathrm{Beta}(2,2)\) ("probably somewhere near half"). Observing \(h\) heads and \(t\) tails makes the posterior exactly \(\mathrm{Beta}(2+h,\,2+t)\) — a pairing in which prior and posterior stay in the same family is called a conjugate prior, and thanks to it the update finishes with parameter addition and no integral at all.</p><p class="interp"><strong>In machine learning.</strong> Bayesian neural networks put a posterior on the weights, and uncertainty estimation and active learning are built on top of that. The more common practical use is the MAP of the next section — take only the maximum instead of computing the whole posterior, and the problem becomes a familiar optimization with a regularization term attached.</p>{{formula}}<p class="hint">Press "Accumulate observations." The violet posterior starts from the grey dashed prior and narrows around the true value (green line) as observations pile up. Compare how far the posterior mean and the MLE differ when there are five or fewer observations, and how close they become by about fifty.</p>`,
    },

    map: {
      title: 'Priors and MAP',
      lead: 'MAP estimation, which takes the maximum of the posterior, is an optimization with one extra term — the prior — added to the likelihood. A Gaussian prior becomes L2 regularization and a Laplace prior becomes L1, and only the latter drives solutions exactly to zero.',
      body: String.raw`<p class="definition"><strong>Definition.</strong> Taking only the single maximum of the posterior rather than handling the whole distribution is maximum a posteriori (MAP) estimation: \(\hat\theta_{\text{MAP}}=\arg\max_\theta P(x\mid\theta)P(\theta)\). The evidence \(P(x)\) does not involve \(\theta\), so it has no effect on the argmax and can be dropped.</p><p class="interp"><strong>The difference between MLE and MAP.</strong> Both are point estimates picking a single parameter, but the material for the judgement differs. MLE maximizes \(P(x\mid\theta)\) alone and picks the value fitting the data best; MAP multiplies in the prior \(P(\theta)\) and picks a value that both fits the data and is plausible under prior knowledge. With little data the prior does much to stabilize MAP, and when there is a lot of data and the likelihood becomes very peaked, MLE and MAP usually come close. Note, though, that MAP keeps only a single maximum rather than the entire posterior, so it does not preserve the full uncertainty the way complete Bayesian inference does.</p><p class="interp"><strong>Take a log and regularization appears.</strong> Following the habit of the earlier section, taking the negative log splits the product into a sum: \(-\log P(x\mid\theta)-\log P(\theta)\). The first term is the NLL we already know (the data term), and the second is the regularization term. Regularization is not a device bolted on afterwards for the sake of optimization; it is a term that follows automatically the moment we hold a prior.</p><p class="interp"><strong>Gaussian prior = L2 = ridge.</strong> Setting \(P(\theta)=\mathcal{N}(0,\tau^2)\) gives \(-\log P(\theta)=\frac{\theta^2}{2\tau^2}+\text{const}\), that is \(\frac{\lambda}{2}\|\theta\|^2\). In the one-dimensional squared-error example on the right the solution shrinks proportionally as \(\hat\theta=\hat\theta_{\text{MLE}}/(1+\lambda)\). This closed form is specific to that example's scaling and is not a formula that applies unchanged to every model. In general, though, L2 tends to shrink parameters smoothly toward zero.</p><p class="interp"><strong>Laplace prior = L1 = lasso.</strong> Setting \(P(\theta)\propto e^{-|\theta|/b}\) makes the penalty \(\lambda|\theta|\). In the same one-dimensional squared-error example the solution takes the soft-thresholding form \(\hat\theta=\mathrm{sign}(\hat\theta_{\text{MLE}})\max(0,\,|\hat\theta_{\text{MLE}}|-\lambda)\), reaching exactly zero once \(\lambda\) is large enough. The exact scale of the coefficients depends on how the objective is defined, but the property that L1 induces sparse solutions because of the sharp corner at the origin holds regardless.</p><p class="interp"><strong>MLE is a special case of MAP.</strong> Make the prior flat (uniform) and \(-\log P(\theta)\) becomes a constant and disappears, and MAP falls back to MLE. Turning regularization off (\(\lambda=0\)) is the same statement as declaring "I prefer all parameter values equally."</p>{{formula}}<p class="hint">Raise λ from 0 and alternate between the two priors. Under the Gaussian the green solution only ever approaches zero, but under the Laplace it reaches exactly zero the moment λ passes 1.6. The kink in the violet objective curve at the origin is the cause.</p>`,
    },
  },

  // Applied outside <pre> blocks: demo labels, buttons, and the initial text of
  // each readout.
  ui: [
    ['Python 코드로 보기', 'View Python code'],
    // random variables
    ['주사위', 'Die'],
    ['클래스 레이블', 'Class label'],
    ['키 (연속)', 'Height (continuous)'],
    // joint / marginal grid
    ['동전 X', 'Coin X'],
    ['합격 여부 Y', 'Pass/fail Y'],
    ['주사위 Z', 'Die Z'],
    ['성공 확률 p', 'success probability p'],
    ['구간 폭 Δ =', 'bin width Δ ='],
    ['CDF 보기', 'Show CDF'],
    ['다시 뽑기', 'Resample'],
    ['역변환 샘플링: uniform u를 CDF로 되돌려 x를 얻는다',
      'Inverse transform sampling: push a uniform u back through the CDF to get x'],
    ['상관 있음', 'Correlated'],
    ['독립으로 보내기', 'Force independence'],
    ['독립', 'Independent'],
    ['균등', 'Uniform'],
    ['Y를 합쳐 P(X) 얻기', 'Sum out Y to get P(X)'],
    ['X를 합쳐 P(Y) 얻기', 'Sum out X to get P(Y)'],
    ['자기회귀 분해: 토큰 하나씩 조건을 늘려가며 곱한다',
      'Autoregressive decomposition: multiply token by token, growing the condition'],
    ['잔차 P(x,y) − P(x)P(y): 0이 되면 독립',
      'Residual P(x,y) − P(x)P(y): independence when it reaches 0'],
    ['표본 뽑기', 'Draw samples'],
    ['초기화', 'Reset'],
    ['표본평균의 수렴: 표본 수가 늘수록 E[X]에 가까워진다',
      'Convergence of the sample mean: it approaches E[X] as samples accumulate'],
    ['온도 T =', 'temperature T ='],
    ['척도 b =', 'scale b ='],
    ['로그 스케일로 꼬리 보기', 'View tails on a log scale'],
    ['손실 관점: −log p(x). 가우시안은 포물선(MSE), 라플라스는 V자(MAE)',
      'As a loss: −log p(x). The Gaussian is a parabola (MSE), the Laplace a V (MAE)'],
    ['파라미터 μ에 대한 가능도 곡선 L(μ) — 최대점이 MLE 해',
      'Likelihood curve L(μ) over the parameter μ — the maximum is the MLE solution'],
    // MLE
    ['가우시안 → 평균', 'Gaussian → mean'],
    ['라플라스 → 중앙값', 'Laplace → median'],
    ['로그가능도 ℓ(μ) — 최대점(초록)이 MLE 해',
      'Log-likelihood ℓ(μ) — the maximum (green) is the MLE solution'],
    ['이상치 추가', 'Add an outlier'],
    ['데이터 수 N =', 'number of data points N ='],
    // Bayes / MAP
    ['관측 수 =', 'observations ='],
    ['관측 누적 재생', 'Accumulate observations'],
    ['가우시안 사전분포 (L2)', 'Gaussian prior (L2)'],
    ['라플라스 사전분포 (L1)', 'Laplace prior (L1)'],
    ['사전분포 강도 λ =', 'prior strength λ ='],

    // Korean inside \text{...} in the shared equations. The formulas live in
    // content.html and are carried over verbatim, so they are translated here
    // rather than duplicated per locale.
    ['\\text{ 중심}', '\\text{ center}'],
    ['\\text{ 퍼짐}', '\\text{ spread}'],
    ['\\text{(잠재변수 제거)}', '\\text{(marginalizing out the latent)}'],
    ['\\text{(고정된 }', '\\text{(for fixed }'],
    ['\\text{에 대해)}', '\\text{)}'],
    ['\\text{파라미터 수}', '\\text{parameter count}'],
    ['\\text{(항상)}', '\\text{(always)}'],
    ['\\text{(큰 수의 법칙)}', '\\text{(law of large numbers)}'],
    ['\\text{(크로스엔트로피)}', '\\text{(cross-entropy)}'],
    ['\\text{크로스엔트로피}', '\\text{cross-entropy}'],
    ['\\text{가우시안 NLL}', '\\text{Gaussian NLL}'],
    ['\\text{가우시안}', '\\text{Gaussian}'],
    ['\\text{라플라스}', '\\text{Laplace}'],
    ['\\text{ 고정, }', '\\text{ fixed, }'],
    ['\\text{ 의 함수)}', '\\text{ as the variable)}'],
    ['\\text{ 고정}', '\\text{ fixed}'],
    ['\\text{상대빈도}', '\\text{relative frequency}'],
    ['\\text{표본평균}', '\\text{sample mean}'],
    ['\\text{중앙값}', '\\text{median}'],
    ['\\text{카테고리 분포}', '\\text{Categorical}'],
    ['\\text{정규화}', '\\text{regularization}'],
  ],

  // Applied only inside <pre><code>, so identifiers are never touched.
  codeComments: [
    ['# 마지막 점이 이상치', '# the last point is an outlier'],
    ['# 가우시안 MLE: 제곱오차를 최소화 → 표본평균',
      '# Gaussian MLE: minimize squared error → sample mean'],
    ['# 라플라스 MLE: 절대오차를 최소화 → 중앙값',
      '# Laplace MLE: minimize absolute error → median'],
    ['# 베르누이 MLE는 그냥 상대빈도',
      '# the Bernoulli MLE is just the relative frequency'],
  ],
};
